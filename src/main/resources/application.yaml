server:
  port: 8080


# application.yml 文件中的配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/check
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect