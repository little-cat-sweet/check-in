<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongyun.mapper.TargetMapper">
    <insert id="addTarget" parameterType="com.hongyun.entity.Target">
        insert into target (user_id, name, description, create_time, day)
        values (#{userId}, #{name}, #{description}, #{createTime}, #{day});
    </insert>

    <resultMap id="targetResultMap" type="com.hongyun.entity.Target">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="createTime" column="create_time"/>
        <result property="day" column="day"/>
    </resultMap>

    <select id="findByUserId" resultMap="targetResultMap" parameterType="map">
        SELECT *
        FROM target
        WHERE user_id = #{userId}
    </select>

    <select id="findAll" resultMap="targetResultMap" parameterType="map">
        SELECT *
        FROM target
    </select>

    <update id="updateBy" parameterType="map">
        UPDATE target
        SET name = #{name}, update_time = now()
        WHERE id = #{id}
    </update>

    <delete id="deleteBy" parameterType="map">
        delete from target where id = #{id}
    </delete>

</mapper>